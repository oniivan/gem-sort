{
  "name": "spicetify-gem-sort",
  "version": "0.5.4",
  "private": true,
  "description": "Spotify-reported play counts and primary-artist Top 10 ranks in Spicetify playlist rows.",
  "scripts": {
    "check": "node --check gem-sort.js && sh -n install.sh",
    "test": "node --test"
  },
  "license": "MIT"
}
